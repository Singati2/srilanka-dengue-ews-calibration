# ABSTRACT_SENTENCE_DERIVATION — B=1000 numbers → exact sentences

**B=1000 inputs:** SL recalibrated matched dev-inclusive = [−0.0002, +0.0302] (includes 0); SL raw matched dev-inclusive = [−0.0079, +0.0245] (includes 0); conditional CIs unchanged (recal [+0.0066,+0.0257], raw [−0.0015,+0.0188]); Colombia dev-inclusive [−0.0001, +0.0244] (includes 0).

**Abstract (methods+findings), recalibration sentence →**
> "… +0.0157 after past-only recalibration of both matched models (95% conditional confidence interval +0.0066 to +0.0257; development-inclusive interval refitting both models −0.0002 to +0.0302). The recalibrated increment excluded zero under conditional resampling but included zero once both models were refit (development-inclusive), and the raw increment included zero under both, so the exclusion held only under conditional resampling and did not survive model-development uncertainty."

**Abstract (conclusions) →**
> "In neither setting did the matched increment robustly exclude zero once model-development uncertainty was included (development-inclusive intervals: Sri Lanka −0.0002 to +0.0302, Colombia −0.0001 to +0.0244); the exclusions held only under conditional resampling, so none was robust across both raw evaluation and model refitting in either setting."

**§3.8 cross-setting synthesis (cross-tier) →**
> "Both nominal exclusions hold only under conditional resampling and dissolve once the models are refit: Colombia's conditional exclusion becomes development-inclusive −0.0001 to +0.0244, and Sri Lanka's recalibrated conditional exclusion becomes development-inclusive −0.0002 to +0.0302 (26 clusters, B=1000). Sri Lanka's exclusion additionally requires recalibration; under raw predictions neither setting excludes zero. No exclusion is robust across both raw evaluation and model refitting in either setting."

**Line-~615 caveat →**
> "… holds only under conditional resampling; once both models are refit it includes zero in both settings (development-inclusive: Sri Lanka −0.0002 to +0.0302, Colombia −0.0001 to +0.0244)."

**Sri Lanka matched table (Table 6):** development-inclusive column updated to raw −0.0079 to +0.0245 and recalibrated −0.0002 to +0.0302; caption: "excludes zero under conditional resampling but includes zero under the development-inclusive treatment, as does the raw increment (B=1000)."

The headline is the symmetric statement, not a per-setting marginal survival: **no exclusion is robust across both raw evaluation and refitting in either setting.**
