# Cover-letter revision summary (thematic)

We thank the editors and reviewers. This revision corrects our own description and interpretation of the model contrasts; it does not alter the previously frozen fitted-model predictions. Key changes:

1. Corrected the Sri Lanka M1 description (structured: recent cases + seasonal harmonics + RDHS fixed effects), distinct by design from Colombia's cases-only M1.
2. Designated the specification-matched contrast M5 − M5_no-climate the principal interpretable climate estimand in both settings, **disclosed as post hoc / exploratory** (not part of the design-locked ladder).
3. Retained the prespecified M4 − M1 as a secondary, non-nested diagnostic that does not isolate climate.
4. Clarified that M5 − M1 is not specification-matched (different penalization/standardization of the frozen M1).
5. Computed the previously missing **recalibrated matched** contrast: recal(M5) − recal(M5_no-climate) = +0.0157 (95% CI +0.0066 to +0.0257, excluding zero). The Sri Lanka matched increment is not distinguishable from zero raw and separates from zero only after past-only recalibration.
6. Corrected the cross-setting reading: consistent in sign, not magnitude; the "Sri Lanka null vs Colombia positive" framing is withdrawn.
7. Added the past-only recalibration alongside the optimistic cross-fit; disclosed conditional-bootstrap and finite-cluster limitations and the provenance qualification (untracked frozen artifacts + attestation).

Full point-by-point mapping is provided separately.
