# Task 4 — Author confirmation (prepare only)

Closeout is documentarily settled but a one-line human confirmation removes all residual doubt on intent + provenance:

> **"Was Sri Lanka M1 specified as cases-only, or as cases + seasonality + RDHS — and if structured, was that specification fixed before the frozen results were produced?"**

- Clause 1 (structure): git + spec answer **structured** (cases + harmonics + RDHS FE).
- Clause 2 (provenance): git DAG + filesystem mtimes + the hash-pinned report answer **fixed before results** (spec committed 13:24; predictions generated 13:32, same day). Author confirmation converts CONFIRMED-on-documents into CONFIRMED-by-author.

Recipients: Khadka / Thapa / Shiwakoti. The closeout verdict (C2-CONFIRMED) does not block the science on this, but submission should carry the authors' explicit sign-off.
