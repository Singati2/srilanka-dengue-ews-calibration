# Reviewer-response draft (specification discrepancy)

During implementation of the requested specification-matched decomposition we identified a discrepancy between the manuscript's stated Sri Lanka M1 specification (recent-case lags only) and the frozen pipeline. We reconstructed the frozen pipeline from the base linked dataset and committed scripts and reproduced the frozen M1/M4/M5 test predictions to <1e-6 (achieved ~1e-16, row identity verified). We then adjudicated whether the Sri Lanka M1 non-climate structure was intended or a defect using evidence independent of the code's runtime behaviour. The dated pre-computation design-lock (hybrid_model_extension_spec.md) **explicitly specifies Sri Lanka M1 as "incidence lags + harmonics + RDHS FE"**, while Colombia's ladder spec specifies its M1 as cases-only. We therefore adjudicated the structure as **intended** and corrected the manuscript's M1 description (country-specific ladder).

Because Sri Lanka's M1 already carries seasonal and RDHS structure, its M5−M1 contrast is approximately the specification-matched climate increment; we make the specification-matched contrast (M5 − M5_no-climate) the primary climate estimand in both countries and retain M4 − M1 as a design-locked, secondary, **non-nested / structurally mismatched** diagnostic that does not isolate climate's incremental contribution. We add the past-only rolling recalibration (verified leakage-free) alongside the optimistic cross-fit. We disclose that the two countries' M1 baselines differ, so M1-level benchmark comparisons are non-comparable and cross-setting statements are confined to the matched estimand. Uncertainty limitations (evaluation-only bootstrap, 26 clusters, recalibration dependence, predictive-not-causal) are stated. Verdict: C2 (REFRAME, qualified). Author confirmation of the Sri Lanka M1 specification is requested to finalize.
